
//if there is delay in network so we we will not get userData then Optional chaining useful
let userData={
    name:'naushad',
    // job:{title:'SE',description:'System engineer'}
};

console.log(userData?.job?.title);